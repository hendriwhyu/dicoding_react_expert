import FormModal from '../components/FormModal';

const stories = {
  title: 'FormModal',
  component: FormModal,
};

export default stories;

const TemplateStory = (args) => <FormModal {...args} />;
